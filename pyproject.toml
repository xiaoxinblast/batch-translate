[project]
name = "batch-translate-toolkit"
version = "10.0.0"
requires-python = ">=3.10"
dependencies = [
    "lxml>=5.0,<7",
    "openpyxl>=3.1,<4",
    "python-docx>=1.1,<2",
]

[tool.unittest]
test-directory = "tests"
